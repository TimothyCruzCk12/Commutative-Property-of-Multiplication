/** @type {import('next').NextConfig} */

const nextConfig = {
  output: 'export',
  basePath: '/Commutative-Property-of-Multiplication',
  images: {
    unoptimized: true
  }
};

export default nextConfig;
